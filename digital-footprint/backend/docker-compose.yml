version: "3.8"

services:
  web:
    build: .
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=sqlite:///whoami.db
    ports:
      - "5000:5000"
    command: python app.py
  
  # Uncomment for async background tasks with Celery
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
