import React from 'react';
import Buttonn from './Buttonn';
import './Scan.css'; // Assuming you have a CSS file for styling

const Scan = () => {
    return (
        <div className="scan">
            <Buttonn text='Scan'/>
            
        </div>
    );
};

export default Scan;